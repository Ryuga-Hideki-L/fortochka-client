# Fortochka — задачи (из аудита + хотелки)

_Живой трекер. Обновлять по мере. Полное обоснование — в docs/MASTER-plan.md._

## ✅ Сделано (эта сессия)
- Мульти-протокол клиент: vless + hysteria2 + tuic, авто-failover (urltest, пул = UDP+CDN).
- Серверы Hysteria2 (UDP:443, salamander) + TUIC (UDP:8443), вложены в подписку.
- Диагностические логи (версия, каналы, задержки, переключения) через clash_api (обход прокси, динамич. порт).
- Фиксы: кириллица-subId, @localhost в подписке, fdge accept-loop заморозка, ретрай подписки.
- Панель: кнопка «Ссылки» (локальная отдача, не через Gcore).
- **Reality decoy: yandex → www.microsoft.com / dl.google.com (разные на инбаунды).** ✓ проверено.
- **UDP-буферы rmem/wmem_default → 4МБ** (фикс дропов). ✓
- **Port-hopping Hysteria2: сервер (nftables редирект 20000-40000→443) готов+проверен.** Клиент — в работе.

## 🔧 В работе
- [ ] Port-hopping клиент: парсинг mport + server_ports/hop_interval (код готов), обновить hy2-ссылку в fdge, проверить e2e, релиз.

## 📋 Аудит — сделать (без денег/аккаунтов)
- [ ] **DoH-резолв домена подписки в клиенте** (резолвить vpn.rungvard.net через DoH, а не системный DNS — против DNS-блока). #2
- [ ] **Проверить/выставить flow=xtls-rprx-vision + fp на Reality-инбаундах** (не полагаться на дефолт 3x-ui). косяк #10
- [ ] **Убрать sslip.io для hy2** — свой домен ИЛИ self-signed+pinned (hy2 умеет нативно). #4
- [ ] **Разные dest на 3 Reality-инбаунда** — частично сделано (microsoft/google/microsoft); сделать 3 разных.
- [ ] **Embedded fallback-эндпоинты в клиент** — инжект через CI-secret (НЕ в публичный исходник!), если подписка недоступна N попыток → падать на них. #5
- [ ] **Второй канал доставки подписки** — Telegram-бот / приватный источник (не через Gcore). #6
- [ ] Панель на том же IP — рассмотреть вынос/allowlist (скан 3x-ui тянет репутацию IP). косяк #7

## 💰 Аудит — нужны деньги/аккаунты юзера (согласовать)
- [ ] **Разнести протоколы по IP / 2-й IP** (OVH Additional IP или лучше другой провайдер) — проверить гипотезу IP-репутации, вернуть Reality в авто. #7
- [ ] **Второй CDN** (CloudFront — ещё держит fronting) параллельно Gcore. #9
- [ ] **Второй VPS на не-OVH ASN** — реальный второй сетевой путь. #10
- [ ] (опц.) Gcore платный tier — снять L7-фильтрацию + origin shielding.

## 🆕 Фичи (хотелки)
- [ ] **Вкладка «Запрет»**: движок ByeDPI (без драйвера, уживается с VPN) ⇄ Zapret/winws (мощнее, драйвер, вместо VPN) — переключаемо.
- [ ]   → выбор разных **стратегий/пресетов** в «Запрете» (понять какая лучше под ISP).
- [ ]   → списки хостов обновляемые (ТСПУ меняется ~ежемесячно).
- [ ] **Кнопка «Тест»** — прогон проверки: подключение живо, активный канал, задержки по каналам, утечки. (клиентский smoketest для юзера)
- [ ] Показать версию на сайте динамически (уже есть ver.js — проверить).

## 🧪 Проверка вживую из РФ (юзеру/друзьям)
- globalcheck.net (что блокируется), whoer.net/2ip.ru (мой IP = 141.95.67.176?), browserleaks.com/dns (утечки).
- Reality теперь может ожить как основной — проверить у друзей после смены decoy.

## 🖥️ Ops
- Стенд: `bash ~/fortochka-ops/smoketest.sh` (17 проверок) — гонять перед раздачей.
- Ротация: сменить пароль панели (1978) + SSH-пароль Кента (утекали в git-историю, вычищены).
